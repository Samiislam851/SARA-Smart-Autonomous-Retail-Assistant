services:
  mongo:
    image: mongo:7
    container_name: nextcart-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - nextcart-mongo-data:/data/db

volumes:
  nextcart-mongo-data:
